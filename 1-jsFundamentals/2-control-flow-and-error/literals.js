//literals


let weekDays = ['mon', 'tue', 'wed', 'th', 'fri']
console.log(weekDays.toString());
//tostring is a method that turns items into a string
console.log(weekDays[3]);

//object
let soup = {
    mon: beef,
    tues: chix,
    wed: noodle,
    th: pea
}

//arrays and object literals can hold primitive and complex types

//primitive = boolean,number
//complex = array, objects, function

//complex arrays and objects can hold both primitive and complex data types